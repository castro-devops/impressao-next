import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};
module.exports = {
  productionBrowserSourceMaps: false, // Desabilita o trace
};

export default nextConfig;
